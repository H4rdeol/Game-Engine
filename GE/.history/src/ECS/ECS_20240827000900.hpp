/*
** EPITECH PROJECT, 2024
** GE [WSL: Ubuntu]
** File description:
** ECS.hpp
*/

#pragma once

#include <vector>
#include <memory>
#include <cassert>
// ? A templated class or abstract class to easily create mapper

namespace ECS
{
    class  {
        public:
            () = default;
            ~() = default;
            (const  &cpy) = delete;
             &operator=(const  &src) = delete;
            ( &&src) = delete;
             &operator=( &&src) = delete;
    };
}
